{
  "name": "yiaga-website-root",
  "version": "1.0.0",
  "description": "Root project for Yiaga website to run frontend and backend together",
  "scripts": {
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && go run main.go",
    "install:all": "cd frontend && npm install && cd ../backend && go mod tidy"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
