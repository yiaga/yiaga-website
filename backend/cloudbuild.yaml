steps:
  # Step 1: Build the container image
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'gcr.io/$PROJECT_ID/yiaga-backend:$COMMIT_SHA', '.']
    dir: 'backend'

  # Step 2: Push the image to Artifact Registry
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/$PROJECT_ID/yiaga-backend:$COMMIT_SHA']

  # Step 3: Deploy to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'yiaga-backend'
      - '--image'
      - 'gcr.io/$PROJECT_ID/yiaga-backend:$COMMIT_SHA'
      - '--region'
      - 'africa-south1'
      - '--platform'
      - 'managed'

images:
  - 'gcr.io/$PROJECT_ID/yiaga-backend:$COMMIT_SHA'

options:
  logging: CLOUD_LOGGING_ONLY

